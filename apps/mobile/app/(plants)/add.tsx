import { Text, View } from "react-native";

export default function AddPlant() {
  return (
    <View style={{ flex: 1, justifyContent:"center", alignItems:"center" }}>
      <Text>Add Plant (placeholder — will open camera later)</Text>
    </View>
  );
}